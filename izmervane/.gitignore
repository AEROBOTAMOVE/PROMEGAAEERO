# 05.09 · МЕТОДЪТ влиза в repo-то, ДАННИТЕ — не.
# Правилото на проекта («произход_на_числата»): скриптът, произвел число в
# backtest_stats.json или в карта, ТРЯБВА да е тук и проверим. Лентата (~2GB)
# и кешираните .npy (650MB) не могат и не бива — те се пресъздават от
# geom_harness.load_tape() за под минута.
_cache/
__pycache__/
*.npy
*.npz
*.parquet
