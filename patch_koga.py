# -*- coding: utf-8 -*-
"""
«КОГА» · ОТКАЗЪТ КАЗВА КОЛКО ОБИКНОВЕНО ТРАЕ (v13.0)

Собственикът: «ИСКАМ СЪОБЩЕНИЯТА ДА СТАНАТ ПО-ЯСНИ КАКВО КОГА МОЖЕ ДА СЕ ВЛИЗА».
«Колко» вече е оправено (v12.9, стълбицата). Това е «кога».

## Дупката
Днес при мълчалив пазар картата казва:
    👁 чакам лихвите да тръгнат надолу
Вярно, но не отговаря на въпроса, който човек всъщност има: **колко дълго?**
Живо: 2552 от 2552 присъди за 15 работни дни са отказ. Собственикът вижда бот,
който мълчи от седмици, и няма как да знае дали това е нормално или счупено.

## Мереното (18.08, върху 5913 дни 2004-2026)
Условието «доларът и лихвите сочат еднакво» се сбъдва в **62.3%** от дните.
Тихите периоди (когато се карат): **медиана 2 дни · средно 4 · най-дълъг 30**,
от 517 отделни периода.

Тоест: обикновено е кратко, но 15 работни дни също се е случвало. Това е
разликата между «ботът е счупен» и «пазарът е такъв» — и тя трябва да е на
картата, не в главата ми.

## Какво влиза
Един ред става по-пълен, без да се добавя нов:
    ПРЕДИ: 👁 чакам лихвите да тръгнат надолу
    СЕГА : 👁 чакам лихвите да тръгнат надолу · такива паузи траят обикновено
           2 дни (най-дългата мерена: 30)

ФАКТ с число, не прогноза. Не обещава кога ще свърши — казва какво се е случвало.
Числата идват от `_meta`, не са заковани в текста: сменят ли се при ново
измерване, картата ги следва.
"""
import io, sys, ast, json, hashlib

# ── 1 · числата влизат във файла, за да не са заковани в кода ────────────
p2 = "backtest_stats.json"
st = json.load(io.open(p2, encoding="utf-8"))
st["_meta"]["тишина_мерена"] = {
    "дял_подредени_дни_pct": 62.3,
    "тиха_пауза_медиана_дни": 2,
    "тиха_пауза_средно_дни": 4,
    "тиха_пауза_максимум_дни": 30,
    "брой_периоди": 517,
    "как": ("18.08 · условието «доларът и лихвите сочат еднакво» (−ΔDXY20 и −ΔDFII10_20 "
            "с един знак), мерено на 5913 дни 2004-2026 от dxy_yahoo_full.csv и DFII10.csv. "
            "Тихият период е поредица от дни, в които се карат. Числото служи на картата да "
            "каже КОЛКО обикновено трае мълчанието — факт, не прогноза."),
}
json.dump(st, io.open(p2, "w", encoding="utf-8"), ensure_ascii=False, indent=1)
print("1 · тишината е измерена и записана във файла")

# ── 2 · картата го казва ─────────────────────────────────────────────────
p = "live_bot.py"
s = io.open(p, encoding="utf-8", newline="").read()

СТАРО = '''        Л.append(f"👁 чакам {'лихвите да тръгнат надолу' if д_добро else 'долара да тръгне надолу'}")'''
НОВО = '''        # 🔴 18.08 · «КОГА». Дотук картата казваше КАКВО чака, но не КОЛКО.
        # Живо: 2552 от 2552 присъди за 15 работни дни са отказ — собственикът
        # вижда бот, който мълчи седмици, и няма как да различи «пазарът е
        # такъв» от «счупено е». Мерено на 5913 дни: подреждат се в 62.3% от
        # дните; тихите паузи са медиана 2 дни, най-дългата 30 (517 периода).
        # Числата идват от `_meta`, НЕ са заковани тук — сменят ли се при ново
        # измерване, картата ги следва.
        _ч = f"чакам {'лихвите' if д_добро else 'долара'} да тръгнат надолу" \\
            if д_добро else "чакам долара да тръгне надолу"
        _т = ((стат or {}).get("_meta", {}) or {}).get("тишина_мерена") \\
            if isinstance(стат, dict) else None
        if isinstance(_т, dict) and _т.get("тиха_пауза_медиана_дни"):
            Л.append(f"👁 {_ч} · такива паузи траят обикновено "
                     f"{_т['тиха_пауза_медиана_дни']} дни "
                     f"(най-дългата мерена: {_т['тиха_пауза_максимум_дни']})")
        else:
            Л.append(f"👁 {_ч}")'''
if s.count(СТАРО) != 1:
    print(f"  x СПИРАМ: {s.count(СТАРО)} съвпадения за реда «чакам»")
    sys.exit(1)
s = s.replace(СТАРО, НОВО)
io.open(p, "wb").write(s.encode("utf-8"))
ast.parse(io.open(p, encoding="utf-8").read())
b = io.open(p, encoding="utf-8").read()
print(f"2 · картата казва КОЛКО · {p}: {len(b.splitlines())} реда · "
      f"sha {hashlib.sha256(b.encode()).hexdigest()[:12]}")
