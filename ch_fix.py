# -*- coding: utf-8 -*-
import sys; sys.argv=["x"]
import live_bot as lb, pandas as pd
PIP=lb.PIP; ДНИ_МАКС=30
ИМЕ={"долар":"доларът","лихви":"лихвите"}
def реш_ред(реш, зад, direction):
    имена=" и ".join(ИМЕ[k] for k in реш); гл="сочи" if len(реш)==1 else "сочат"
    if зад==len(реш): return f"📌 {имена} още {гл} натам · нищо не се е развалило"
    if зад==0: return f"⚠️ {имена} вече {гл} СРЕЩУ тази посока"
    return "⚠️ доларът и лихвите вече не сочат на една страна"
print(реш_ред(["долар"],1,"long")); print(реш_ред(["долар","лихви"],2,"long")); print(реш_ред(["долар","лихви"],0,"long"))
def трети(n):
    ост=3-n
    return "остава последната трета" if ост==1 else f"остават още {ост} трети" if ост==2 else "всичко е прибрано"
print(трети(1)); print(трети(2)); print(трети(3))
print(lb.СРЕБРО_ВХОД)
